/* Implemente uma função que receba dois inteiros e retorne o maior entre eles. A assinatura deve ser:

int maior(int a, int b);

Não é necessário escrever a função main, apenas a função acima. */

int maior (int a, int b){
	return (a > b ? a : b);
}